/**
 * Main-content extraction for generic HTML pages (design §6.1) — Readability
 * over a DOM. Stub: arXiv and feed sources supply abstracts directly, so this
 * is only needed for blog/news pages (Phase 4). Implement with
 * `@mozilla/readability` over a `@b-fuze/deno-dom` document.
 */
export function extractMainText(_html: string, _url: string): string {
  throw new Error("extractMainText not implemented (Phase 4)");
}
